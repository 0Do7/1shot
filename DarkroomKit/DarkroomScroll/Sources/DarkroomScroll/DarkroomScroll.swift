/// Scrolling-capture engine: scroll synthesis, frame acquisition, overlap-correlation stitcher, seam model for restitch
/// (design D8).
public enum DarkroomScrollInfo {
    /// Scaffold marker; replaced by real API as lane tasks land.
    public static let packageName = "DarkroomScroll"
}
