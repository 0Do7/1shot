/// GRDB + FTS5 library store, auto-import watcher, Spotlight donation, retention (design D4).
public enum DarkroomLibraryInfo {
    /// Scaffold marker; replaced by real API as lane tasks land.
    public static let packageName = "DarkroomLibrary"
}
