/// Portable domain model: annotation documents, geometry, naming heuristics, library domain types, destination
/// protocol, settings model. Foundation only — never AppKit/SwiftUI/UIKit (CI-enforced, design D2).
public enum DarkroomCoreInfo {
    /// Scaffold marker; replaced by real API as lane tasks land.
    public static let packageName = "DarkroomCore"
}
